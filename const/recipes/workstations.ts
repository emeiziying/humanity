import type { RecipePrototype } from 'humanity'

const workstations: RecipePrototype[] = [
  {
    type: 'recipe',
    name: 'Temporary logging site',
    total_cost: 10,
    ingredients: [],
    results: [{ type: 'item', name: 'Temporary logging site', amount: 1 }],
  },
]

export default workstations
